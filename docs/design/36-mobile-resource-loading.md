# 手机资源加载修复

2026-09-13：用户确认旧部署 6b653344 手机无代理可用，新入口首屏之后持续载入。未把电脑代理访问成功视为手机直连验收。

62 个未变化的图片、音频、视频与字体均核对了旧版 Git blob 和当前 SHA256，生产资源读取改用该已知可用部署；CSS、SVG 插画、预加载、场景、开场和音频统一解析。新脚本和新增/变更素材仍使用新部署。构建每次核对指纹，防止后续更新错误读取旧素材。不要删除 6b653344 部署，它承载这版共享资源。

移除外层 iframe 页面重复预加载；进入开场不再等待全部场景下载，游戏控件仍在场景资源完整后初始化；背景与战斗音频异步恢复，不持有入口锁；字体超时先使用已有字体；图片失败重试避开失败缓存。

构建通过、5 项资源路由检查通过。手机网络结果仍以用户设备验证为准，不声称完成手机实测。
