# 完整预加载的传输修复

旧 6b653344 成功包和当前 62630b23 的图片、视频、音频逐字节一致；旧包同样存在三路图片队列和 20 秒固定超时。不能声称新增玩法扩大素材，或把电脑连通性当作手机验证。

现有图片超时覆盖下载加解码全过程：即使持续收到数据，20 秒仍会取消并从头重试，三次约一分钟。音频与视频也按总时长 25 秒中止。不同部署子域名之间不共享浏览器缓存。以上是代码和发布记录可确认的风险；手机本次具体失败文件尚不可见。

修复保持全部素材就绪才可进入：下载超时按无数据活动计算；下载和解码分别限流；独立音频并行预载；错误保留文件名与阶段。后续固定生产分享入口，不再要求玩家更换部署子域名。仅检查资源加载边界和构建，不执行全量游戏测试。
